# Mahou (), Jujutsu's scripting language


This directory contains the core types and the interpreter as the entry-point
to it. It's factored out into a separate crate to make `jj-lib` smaller in both
binary size and compile times and so downstreams compile it out if needed.


### Types

TODO:


### Traits
